package lodVader.integration.mongodb;

import org.junit.Test;

import junit.framework.Assert;
import lodVader.configuration.LODVaderProperties;
import lodVader.mongodb.DBSuperClass2;

public class DBVersionTest {

	@Test
	public void versionTest() {
//		Assert.assertTrue(DBSuperClass2.getDBInstance().getMongo().MAJOR_VERSION >= LODVaderProperties.MONGODB_MINIMUM_MAJOR_VERSION);
//		Assert.assertTrue(DBSuperClass2.getDBInstance().getMongo().MINOR_VERSION >= LODVaderProperties.MONGODB_MINIMUM_MINOR_VERSION);
	}

}
